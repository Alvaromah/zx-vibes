# Plans

Planning outputs that propose tasks but do not execute them.

When the user selects tasks from a plan, convert those selected items into real task files under `../pending/` and add them to `../queue.md`. The plan file stays here for traceability.
