---
"create-zx-vibes": patch
---

Avoid Node's Windows shell-argument deprecation warning when `--install` runs `pnpm install`, and make the post-create guidance reflect whether dependencies were installed.
