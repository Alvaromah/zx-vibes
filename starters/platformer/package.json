{
  "name": "__NAME__",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "zxs build",
    "run": "zxs run --bin build/main.bin --org 0x8000 --frames 300 --screenshot screen.png",
    "test": "zxs test tests",
    "verify": "zxs verify",
    "preview": "zxs preview"
  },
  "devDependencies": {
    "zx-vibes": "^0.1.0"
  }
}
