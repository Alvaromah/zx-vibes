# Spectral

Read `AGENTS.md` (canonical agent instructions: commands, critical rules,
harness protocol) and then `.harness/state.md` (current truth). The
`.harness/` directory is the cross-session memory — keep it updated per the
lifecycle in AGENTS.md.
